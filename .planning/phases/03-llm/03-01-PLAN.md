---
phase: 03-llm
plan: '01'
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/src/llm/mod.rs
  - src-tauri/src/llm/openai.rs
  - src-tauri/src/llm/prompts.rs
  - src-tauri/src/commands/llm.rs
  - src-tauri/src/commands/mod.rs
  - src-tauri/src/lib.rs
  - src-tauri/Cargo.toml
autonomous: true
requirements: []
user_setup:
  - service: openai
    why: "LLM API provider"
    env_vars: []
    dashboard_config:
      - task: "Create OpenAI API key"
        location: "https://platform.openai.com/api-keys"

must_haves:
  truths:
    - "User can configure OpenAI API key in settings"
    - "API key is stored encrypted locally"
    - "User can send messages and receive streaming responses"
    - "Project context is automatically included in prompts"
  artifacts:
    - path: "src-tauri/src/llm/mod.rs"
      provides: "LLM module initialization"
    - path: "src-tauri/src/llm/openai.rs"
      provides: "OpenAI API client with streaming"
    - path: "src-tauri/src/llm/prompts.rs"
      provides: "Prompt templates with project context"
    - path: "src-tauri/src/commands/llm.rs"
      provides: "Tauri commands for LLM operations"
  key_links:
    - from: "src-tauri/src/commands/llm.rs"
      to: "src-tauri/src/llm/openai.rs"
      via: "OpenAIClient::new()"
      pattern: "llm::openai"
    - from: "src-tauri/src/commands/llm.rs"
      to: "src-tauri/src/db/mod.rs"
      via: "Database State"
      pattern: "db\\.get_project"
---

<objective>
Implement Rust backend for LLM integration with OpenAI API, including secure API key storage, streaming response handling, and project context injection.
</objective>

<context>
@D:\Projects\Tauri\ProjectFlow\.planning\phases\03-llm\03-RESEARCH.md
@D:\Projects\Tauri\ProjectFlow\.planning\phases\03-llm\03-CONTEXT.md
@D:\Projects\Tauri\ProjectFlow\src-tauri\src\commands\mod.rs
@D:\Projects\Tauri\ProjectFlow\src-tauri\Cargo.toml
@D:\Projects\Tauri\ProjectFlow\src-tauri\src\lib.rs
@D:\Projects\Tauri\ProjectFlow\src-tauri\src\db\mod.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add LLM dependencies to Cargo.toml</name>
  <files>src-tauri/Cargo.toml</files>
  <read_first>
    - src-tauri/Cargo.toml (existing dependencies)
  </read_first>
  <action>
Add the following dependencies to Cargo.toml:
- reqwest = { version = "0.12", features = ["json", "stream"] }
- lgpt = "0.4"
- aes-gcm = "0.10"
- base64 = "0.22"
- tauri-plugin-store = "2"

These versions are based on RESEARCH.md standard stack recommendations.
  </action>
  <acceptance_criteria>
    <grep>reqwest</grep>
    <grep>lgpt</grep>
    <grep>aes-gcm</grep>
    <grep>tauri-plugin-store</grep>
  </acceptance_criteria>
  <verify>
    <automated>cargo check --manifest-path src-tauri/Cargo.toml 2>&1 | head -20</automated>
  </verify>
  <done>Cargo.toml updated with LLM dependencies</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create LLM module with OpenAI client</name>
  <files>src-tauri/src/llm/mod.rs, src-tauri/src/llm/openai.rs, src-tauri/src/llm/prompts.rs</files>
  <read_first>
    - src-tauri/src/db/mod.rs (for understanding Database struct)
  </read_first>
  <action>
Create src-tauri/src/llm/ directory with:
1. mod.rs - Module initialization, exports OpenAIClient and Message types
2. openai.rs - OpenAI API client with streaming support using reqwest
3. prompts.rs - Prompt templates including build_system_prompt function that injects project context

Key implementation:
- Message struct: { role: "user"|"assistant"|"system", content: String }
- OpenAIClient: new(), stream_chat(messages) -> impl Stream<Item=Result<String>>
- build_system_prompt(project: &Project, tasks: &[Task]) -> String with project name, description, task list
  </action>
  <acceptance_criteria>
    <grep>pub struct OpenAIClient</grep>
    <grep>pub struct Message</grep>
    <grep>pub fn build_system_prompt</grep>
    <grep>stream_chat</grep>
  </acceptance_criteria>
  <verify>
    <automated>cargo check --manifest-path src-tauri/Cargo.toml 2>&1 | head -30</automated>
  </verify>
  <done>LLM module created with OpenAI client and streaming support</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement encrypted API key storage</name>
  <files>src-tauri/src/llm/storage.rs</files>
  <read_first>
    - src-tauri/src/lib.rs (app initialization pattern)
  </read_first>
  <action>
Create src-tauri/src/llm/storage.rs with:
- encrypt_api_key(key: &str) -> Vec<u8> using AES-GCM with machine-specific key
- decrypt_api_key(encrypted: &[u8]) -> String
- store_api_key(app_handle, model: &str, encrypted_key: &[u8]) using tauri-plugin-store
- retrieve_api_key(app_handle, model: &str) -> Option<String>

Use device-specific key derivation: UUID of machine + app name as base for AES key.
  </action>
  <acceptance_criteria>
    <grep>pub fn encrypt_api_key</grep>
    <grep>pub fn decrypt_api_key</grep>
    <grep>pub fn store_api_key</grep>
    <grep>pub fn retrieve_api_key</grep>
  </acceptance_criteria>
  <verify>
    <automated>cargo check --manifest-path src-tauri/Cargo.toml 2>&1 | head -30</automated>
  </verify>
  <done>Encrypted API key storage implemented</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Create Tauri commands for LLM operations</name>
  <files>src-tauri/src/commands/llm.rs, src-tauri/src/commands/mod.rs, src-tauri/src/lib.rs</files>
  <read_first>
    - src-tauri/src/commands/mod.rs (existing command pattern)
    - src-tauri/src/lib.rs (command registration pattern)
  </read_first>
  <action>
Create src-tauri/src/commands/llm.rs with commands:
1. llm_save_key(model: String, api_key: String) - encrypts and stores API key
2. llm_get_key_status(model: String) -> bool - checks if key exists
3. llm_delete_key(model: String) - removes stored key
4. llm_chat(app: AppHandle, db: State<Database>, messages: Vec<Message>, project_id: Option<String>) - streams chat response

The llm_chat command:
- Gets project context if project_id provided (build_system_prompt)
- Calls client.stream_chat()
- Emits "llm-token" event for each chunk
- Emits "llm-done" when complete
- Emits "llm-error" on failure

Update commands/mod.rs to include llm module.
Update lib.rs to register llm commands.
  </action>
  <acceptance_criteria>
    <grep>llm_save_key</grep>
    <grep>llm_get_key_status</grep>
    <grep>llm_delete_key</grep>
    <grep>llm_chat</grep>
    <grep>llm-token</grep>
  </acceptance_criteria>
  <verify>
    <automated>cargo check --manifest-path src-tauri/Cargo.toml 2>&1 | head -30</automated>
  </verify>
  <done>Tauri commands for LLM operations implemented</done>
</task>

</tasks>

<verification>
All Rust code compiles successfully. LLM commands registered with Tauri.
</verification>

<success_criteria>
1. cargo check passes for src-tauri
2. All new modules compile without errors
3. LLM commands are registered in lib.rs
4. API key can be stored and retrieved encrypted
5. Streaming responses work via Tauri events
</success_criteria>

<output>
After completion, create `.planning/phases/03-llm/03-01-SUMMARY.md`
</output>
