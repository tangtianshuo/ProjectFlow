---
phase: 03-llm
plan: '02'
type: execute
wave: 1
depends_on: []
files_modified:
  - src/stores/llmStore.ts
  - src/lib/api.ts
  - src/components/features/llm/LlmPanel.vue
  - src/components/features/llm/ChatMessage.vue
  - src/components/features/llm/LlmSettings.vue
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "User can open LLM panel from sidebar"
    - "User can see chat history with messages"
    - "User can type and send messages"
    - "Streaming responses appear in real-time"
    - "User can configure API key in settings"
  artifacts:
    - path: "src/stores/llmStore.ts"
      provides: "LLM state management (messages, streaming, settings)"
    - path: "src/lib/api.ts"
      provides: "LLM API wrappers (saveKey, deleteKey, getStatus, chat)"
    - path: "src/components/features/llm/LlmPanel.vue"
      provides: "Main LLM chat panel UI"
    - path: "src/components/features/llm/ChatMessage.vue"
      provides: "Individual message component"
    - path: "src/components/features/llm/LlmSettings.vue"
      provides: "API key settings modal"
  key_links:
    - from: "src/stores/llmStore.ts"
      to: "src/lib/api.ts"
      via: "llmApi.chat()"
      pattern: "llmApi\\.(saveKey|chat)"
    - from: "src/components/features/llm/LlmPanel.vue"
      to: "src/stores/llmStore.ts"
      via: "useLlmStore()"
      pattern: "useLlmStore"
---

<objective>
Implement Vue frontend for LLM AI Assistant: chat panel UI, message components, settings modal, and state management with streaming support.
</objective>

<context>
@D:\Projects\Tauri\ProjectFlow\.planning\phases\03-llm\03-CONTEXT.md
@D:\Projects\Tauri\ProjectFlow\.planning\phases\03-llm\03-RESEARCH.md
@D:\Projects\Tauri\ProjectFlow\src\stores\uiStore.ts
@D:\Projects\Tauri\ProjectFlow\src\lib\api.ts
@D:\Projects\Tauri\ProjectFlow\src\components\ui\Input.vue
@D:\Projects\Tauri\ProjectFlow\src\components\ui\Button.vue
@D:\Projects\Tauri\ProjectFlow\src\components\ui\Modal.vue
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add LLM API functions to api.ts</name>
  <files>src/lib/api.ts</files>
  <read_first>
    - src/lib/api.ts (existing API pattern)
  </read_first>
  <action>
Add to src/lib/api.ts:
1. LLM Message type: { role: "user"|"assistant"|"system", content: string }
2. llmApi.saveKey(model: string, apiKey: string): Promise<void>
3. llmApi.getKeyStatus(model: string): Promise<boolean>
4. llmApi.deleteKey(model: string): Promise<void>
5. llmApi.chat(messages: Message[], projectId?: string): Promise<void> - invokes llm_chat command

Use the same pattern as existing API functions (invoke wrapper).
  </action>
  <acceptance_criteria>
    <grep>llmApi</grep>
    <grep>saveKey</grep>
    <grep>getKeyStatus</grep>
    <grep>deleteKey</grep>
    <grep>chat</grep>
  </acceptance_criteria>
  <verify>
    <automated>npm run dev -- --host 2>&1 | head -30</automated>
  </verify>
  <done>LLM API functions added to api.ts</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create LLM store for state management</name>
  <files>src/stores/llmStore.ts</files>
  <read_first>
    - src/stores/uiStore.ts (existing store pattern)
    - src/stores/projectStore.ts (existing store pattern)
  </read_first>
  <action>
Create src/stores/llmStore.ts using Pinia:
- State:
  - messages: Message[] (chat history)
  - isStreaming: boolean
  - currentStreamingContent: string
  - apiKeyStatus: Record<string, boolean> (key status by model)
  - settingsOpen: boolean
  - selectedModel: string (default: "gpt-4o")

- Actions:
  - addMessage(message: Message): void
  - clearMessages(): void
  - setStreaming(isStreaming: boolean): void
  - appendStreamingContent(content: string): void
  - finishStreaming(): void
  - async sendMessage(content: string, projectId?: string): Promise<void>
  - async saveApiKey(model: string, apiKey: string): Promise<void>
  - async deleteApiKey(model: string): Promise<void>
  - async checkKeyStatus(model: string): Promise<void>

- Setup Tauri event listeners in store initialization:
  - listen("llm-token", (event) => appendStreamingContent)
  - listen("llm-done", () => finishStreaming)
  - listen("llm-error", (event) => handleError)

Import Message type from api.ts.
  </action>
  <acceptance_criteria>
    <grep>export const useLlmStore</grep>
    <grep>messages</grep>
    <grep>isStreaming</grep>
    <grep>sendMessage</grep>
    <grep>listen.*llm-token</grep>
  </acceptance_criteria>
  <verify>
    <automated>npm run dev -- --host 2>&1 | head -30</automated>
  </verify>
  <done>LLM store created with state management</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create LLM UI components</name>
  <files>src/components/features/llm/LlmPanel.vue, src/components/features/llm/ChatMessage.vue, src/components/features/llm/LlmSettings.vue</files>
  <read_first>
    - src/components/layout/Sidebar.vue (existing sidebar structure)
    - src/components/features/dashboard/Dashboard.vue (existing view pattern)
    - src/components/ui/Modal.vue (existing modal)
    - src/components/ui/Input.vue (existing input)
    - src/components/ui/Button.vue (existing button)
  </read_first>
  <action>
Create components/features/llm/ directory:
1. ChatMessage.vue:
   - Props: message: { role, content }
   - Template: display role icon, content with markdown rendering (use marked)
   - User messages: right-aligned, indigo gradient background
   - Assistant messages: left-aligned, secondary background

2. LlmSettings.vue:
   - Modal with API key input
   - Model selector (dropdown with: gpt-4o, gpt-4o-mini)
   - Save/Delete buttons
   - Status indicator (configured/not configured)
   - Emits: close

3. LlmPanel.vue:
   - Main panel: header with title "AI 助手", settings button
   - Message list: scrollable, shows ChatMessage components
   - Input area: textarea with send button, disabled during streaming
   - Empty state: "开始与 AI 助手对话"
   - Streaming indicator: "AI 正在思考..." with pulse animation
   - Use existing UI components (Button, Input, Modal, Icon)

Follow existing component patterns from the codebase.
  </action>
  <acceptance_criteria>
    <grep>ChatMessage</grep>
    <grep>LlmSettings</grep>
    <grep>LlmPanel</grep>
    <grep>marked</grep>
  </acceptance_criteria>
  <verify>
    <automated>npm run dev -- --host 2>&1 | head -30</automated>
  </verify>
  <done>LLM UI components created</done>
</task>

</tasks>

<verification>
Frontend builds without errors. Components render correctly.
</verification>

<success_criteria>
1. npm run dev passes
2. All Vue components compile
3. LLM store integrates with Tauri events
4. API functions work with backend commands
</success_criteria>

<output>
After completion, create `.planning/phases/03-llm/03-02-SUMMARY.md`
</output>
