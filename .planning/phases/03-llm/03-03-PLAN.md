---
phase: 03-llm
plan: '03'
type: execute
wave: 2
depends_on:
  - 03-01
  03-02
files_modified:
  - src/stores/uiStore.ts
  - src/components/layout/Sidebar.vue
  - src/components/features/llm/LlmPanel.vue
  - src/App.vue
autonomous: false
requirements: []
user_setup: []

must_haves:
  truths:
    - "LLM menu item appears in sidebar navigation"
    - "Clicking LLM menu opens the chat panel"
    - "Full chat flow works end-to-end"
  artifacts:
    - path: "src/stores/uiStore.ts"
      provides: "ViewMode includes llm option"
    - path: "src/components/layout/Sidebar.vue"
      provides: "LLM menu item added"
    - path: "src/App.vue"
      provides: "LlmPanel rendered when view is llm"
  key_links:
    - from: "src/components/layout/Sidebar.vue"
      to: "src/stores/uiStore.ts"
      via: "ViewMode type"
      pattern: "ViewMode.*llm"
    - from: "src/App.vue"
      to: "src/components/features/llm/LlmPanel.vue"
      via: "v-if"
      pattern: "currentView.*===.*llm"
---

<objective>
Integrate LLM panel into sidebar navigation, update view modes, and connect full UI flow with backend.
</objective>

<context>
@D:\Projects\Tauri\ProjectFlow\.planning\phases\03-llm\03-01-PLAN.md
@D:\Projects\Tauri\ProjectFlow\.planning\phases\03-llm\03-02-PLAN.md
@D:\Projects\Tauri\ProjectFlow\src\stores\uiStore.ts
@D:\Projects\Tauri\ProjectFlow\src\components\layout\Sidebar.vue
@D:\Projects\Tauri\ProjectFlow\src\App.vue
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update UI store for LLM view mode</name>
  <files>src/stores/uiStore.ts</files>
  <read_first>
    - src/stores/uiStore.ts (existing ViewMode type)
  </read_first>
  <action>
Update ViewMode type in src/stores/uiStore.ts:
- Add "llm" to ViewMode type: type ViewMode = "dashboard" | "projects" | "tasks" | "documents" | "settings" | "recycleBin" | "llm"
- The type is already exported, so update it in place
  </action>
  <acceptance_criteria>
    <grep>ViewMode.*llm</grep>
    <grep>"llm"</grep>
  </acceptance_criteria>
  <verify>
    <automated>npm run dev -- --host 2>&1 | head -30</automated>
  </verify>
  <done>UI store updated with llm view mode</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add LLM menu item to sidebar</name>
  <files>src/components/layout/Sidebar.vue</files>
  <read_first>
    - src/components/layout/Sidebar.vue (existing menu structure)
  </read_first>
  <action>
In Sidebar.vue:
1. Add LLM icon import if needed (use "bot" icon from Lucide)
2. Add LLM menu item to menuItems array:
   { id: "llm", label: "AI 助手", icon: "bot" }

The menu should appear before "回收站" (after documents).
  </action>
  <acceptance_criteria>
    <grep>id: "llm"</grep>
    <grep>label: "AI 助手"</grep>
    <grep>icon: "bot"</grep>
  </acceptance_criteria>
  <verify>
    <automated>npm run dev -- --host 2>&1 | head -30</automated>
  </verify>
  <done>LLM menu item added to sidebar</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Render LLM panel in App.vue</name>
  <files>src/App.vue</files>
  <read_first>
    - src/App.vue (existing view rendering pattern)
  </read_first>
  <action>
In App.vue:
1. Import LlmPanel: import LlmPanel from "./components/features/llm/LlmPanel.vue"
2. Add conditional rendering:
   <LlmPanel v-if="uiStore.currentView === 'llm'" />

Add it after the existing view components (Dashboard, ProjectList, TaskBoard, DocumentCenter, RecycleBin).
  </action>
  <acceptance_criteria>
    <grep>LlmPanel</grep>
    <grep>currentView === 'llm'</grep>
  </acceptance_criteria>
  <verify>
    <automated>npm run dev -- --host 2>&1 | head -30</automated>
  </verify>
  <done>LLM panel renders when llm view is active</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Verify full LLM flow</name>
  <files>src/components/features/llm/LlmPanel.vue, src-tauri/src/commands/llm.rs</files>
  <read_first>
    - src/components/features/llm/LlmPanel.vue
    - src-tauri/src/commands/llm.rs
  </read_first>
  <action>
The task builds are complete. Now verify the full LLM integration works:
1. Ensure Rust backend compiles: cargo check --manifest-path src-tauri/Cargo.toml
2. Ensure frontend compiles: npm run dev
3. Full test: configure API key in settings, send a message, verify streaming response

To verify manually:
- Open the app
- Click "AI 助手" in sidebar
- Click settings icon
- Enter OpenAI API key and save
- Type a message and send
- Verify response streams in real-time
  </action>
  <how-to-verify>
1. Run: npm run tauri dev
2. Click "AI 助手" in sidebar navigation
3. Click gear icon (settings)
4. Enter OpenAI API key (from platform.openai.com)
5. Click save
6. Type "Hello, what can you help me with?" and press send
7. Verify streaming response appears in chat
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
  <done>Full LLM chat flow verified end-to-end</done>
</task>

</tasks>

<verification>
Both frontend and backend compile. LLM integration works end-to-end.
</verification>

<success_criteria>
1. npm run tauri dev starts without errors
2. LLM panel accessible from sidebar
3. API key can be saved and retrieved
4. Chat messages send and receive streaming responses
5. Project context is included in system prompt
</success_criteria>

<output>
After completion, create `.planning/phases/03-llm/03-03-SUMMARY.md`
</output>
