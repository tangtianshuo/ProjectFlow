---
phase: 05-litellm-rs-refactor
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/Cargo.toml
  - src-tauri/src/llm/litellm.rs
autonomous: true
requirements:
  - LLM-10
  - LLM-11
  - LLM-12
  - LLM-13
user_setup: []
must_haves:
  truths:
    - litellm_rs library is added as dependency
    - New LitellmClient struct exists with stream_chat and chat methods
    - ModelConfig unified from storage.rs is used
    - base_url configuration is preserved
  artifacts:
    - path: src-tauri/Cargo.toml
      contains: litellm_rs
    - path: src-tauri/src/llm/litellm.rs
      provides: Unified LLM client using litellm_rs
      exports: LitellmClient, Message
---

<objective>
Add litellm_rs dependency and create new unified LLM client module.

Purpose: Replace custom OpenAI client with litellm_rs for unified API access across multiple LLM providers (OpenAI, Anthropic, Azure, Ollama, and OpenAI-compatible APIs like Kimi/DeepSeek/MiniMax).

Output: New litellm.rs module with LitellmClient struct, updated Cargo.toml
</objective>

<execution_context>
@C:/Users/10345/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/10345/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src-tauri/Cargo.toml
@src-tauri/src/llm/mod.rs
@src-tauri/src/llm/openai.rs
@src-tauri/src/llm/storage.rs

# Key interfaces from existing code
From src-tauri/src/llm/storage.rs:
```rust
pub struct ModelConfig {
    pub base_url: String,
    pub model_name: String,
}
```

From src-tauri/src/llm/openai.rs:
```rust
pub struct OpenAIClient {
    client: Client,
    api_key: String,
    model: String,
    base_url: String,
}

impl OpenAIClient {
    pub fn new(api_key: String, model: Option<String>, base_url: Option<String>) -> Self
    pub async fn stream_chat(&self, messages: Vec<Message>) -> Result<...>
    pub async fn chat(&self, messages: Vec<Message>) -> Result<String, String>
}
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add litellm_rs dependency to Cargo.toml</name>
  <files>src-tauri/Cargo.toml</files>
  <read_first>
    - src-tauri/Cargo.toml
  </read_first>
  <action>
    Add litellm_rs dependency to Cargo.toml. Add the following line in the [dependencies] section under "# LLM dependencies":

    litellm_rs = "0.1"

    Keep all existing LLM dependencies (reqwest, bytes, futures-util, keyring, aes-gcm, base64) as they are still needed for storage.
  </action>
  <verify>
    <automated>grep -n "litellm_rs" src-tauri/Cargo.toml</automated>
  </verify>
  <acceptance_criteria>
    - Cargo.toml contains "litellm_rs" line in [dependencies]
  </acceptance_criteria>
  <done>litellm_rs dependency added to Cargo.toml</done>
</task>

<task type="auto">
  <name>Task 2: Create litellm.rs client module</name>
  <files>src-tauri/src/llm/litellm.rs</files>
  <read_first>
    - src-tauri/src/llm/openai.rs
    - src-tauri/src/llm/storage.rs
  </read_first>
  <action>
    Create new file src-tauri/src/llm/litellm.rs with unified LLM client using litellm_rs.

    The module should:
    1. Re-export Message type from storage/existing pattern (role: String, content: String)
    2. Create LitellmClient struct containing:
       - api_key: String
       - model: String
       - base_url: Option<String> (for OpenAI-compatible APIs)
    3. Implement LitellmClient::new(api_key, model, base_url) -> Self
    4. Implement stream_chat method:
       - Accepts Vec<Message>
       - Returns a stream yielding Result<String, String>
       - Uses litellm_rs::Litellm for API calls
       - Handles base_url for OpenAI-compatible APIs
    5. Implement chat method (non-streaming):
       - Accepts Vec<Message>
       - Returns Result<String, String>

    Key implementation notes:
    - Use litellm_rs::Litellm::new() to create client
    - For base_url, pass custom url to chat_completion if provided (for Kimi/DeepSeek/MiniMax compatibility)
    - Stream handling: map litellm_rs stream chunks to String content
    - Keep error handling simple: convert litellm_rs errors to String
  </action>
  <verify>
    <automated>test -f src-tauri/src/llm/litellm.rs && grep -q "struct LitellmClient" src-tauri/src/llm/litellm.rs</automated>
  </verify>
  <acceptance_criteria>
    - File src-tauri/src/llm/litellm.rs exists
    - Contains "struct LitellmClient" definition
    - Contains "fn new(" constructor
    - Contains "fn stream_chat" method
    - Contains "fn chat" method (non-streaming)
  </acceptance_criteria>
  <done>litellm.rs module created with LitellmClient struct</done>
</task>

</tasks>

<verification>
- cargo check in src-tauri passes with new litellm_rs dependency
- litellm.rs module compiles without errors
</verification>

<success_criteria>
1. litellm_rs added to Cargo.toml
2. litellm.rs created with LitellmClient struct
3. Both stream_chat and chat methods implemented
4. base_url configuration capability preserved
5. cargo check succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/05-litellm-rs-refactor/05-01-SUMMARY.md`
</output>
