---
phase: 02-light-theme
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/App.vue
autonomous: true
gap_closure: true
requirements: []
---

<objective>
Fix App.vue hardcoded dark background that prevents light theme from working.

Purpose: The main app container has a hardcoded dark background color that doesn't switch when the user toggles to light theme. This breaks the light theme feature.

Output: Fixed App.vue with theme-aware background using CSS variable.
</objective>

<context>
@D:\Projects\Tauri\ProjectFlow\src\App.vue
@D:\Projects\Tauri\ProjectFlow\src\style.css

The CSS variable `--bg-primary` is defined in style.css:
- Dark theme (default): `--bg-primary: #08080c` (line 14)
- Light theme: `--bg-primary: #fafafa` (line 70)
</context>

<tasks>

<task type="auto">
  <name>Fix App.vue hardcoded background color</name>
  <files>src/App.vue</files>
  <read_first>
    - src/App.vue (to see exact line 21 that needs modification)
    - src/style.css (to verify --bg-primary CSS variable exists)
  </read_first>
  <action>
    Replace the hardcoded dark background color on line 21 in App.vue.

    Current (line 21):
    ```
    <div class="flex min-h-screen bg-[#08080c]">
    ```

    Change to:
    ```
    <div class="flex min-h-screen bg-[var(--bg-primary)]">
    ```

    This makes the main container use the CSS variable that switches between dark (#08080c) and light (#fafafa) based on the theme.
  </action>
  <acceptance_criteria>
    - Line 21 contains "bg-[var(--bg-primary)]" instead of "bg-[#08080c]"
    - No other hardcoded background colors remain in the div
    - The class attribute still contains "flex min-h-screen"
  </acceptance_criteria>
  <verify>
    <automated>grep -n "bg-\[var(--bg-primary)\]" "D:\Projects\Tauri\ProjectFlow\src\App.vue"</automated>
  </verify>
  <done>App.vue uses theme-aware CSS variable for background, light theme will display correctly when toggled</done>
</task>

</tasks>

<verification>
The fix ensures that when users toggle to light theme:
1. The main app background changes from dark (#08080c) to light (#fafafa)
2. The theme toggle in Sidebar.vue works as expected
3. No hardcoded dark colors remain in the main container
</verification>

<success_criteria>
- App.vue line 21 uses `bg-[var(--bg-primary)]` instead of hardcoded `bg-[#08080c]`
- Light theme background (#fafafa) displays correctly when toggled
- Dark theme continues to work correctly with (#08080c)
</success_criteria>

<output>
After completion, create `.planning/phases/02-light-theme/02-03-SUMMARY.md`
</output>
