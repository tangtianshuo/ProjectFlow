---
phase: 01-dark-ui-optimization
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/Icon.vue
  - src/components/ui/Button.vue
  - src/components/ui/Input.vue
  - src/components/ui/Select.vue
  - src/components/features/tasks/TaskBoard.vue
  - src/components/layout/Sidebar.vue
  - src/components/features/dashboard/Dashboard.vue
  - src/components/features/projects/ProjectList.vue
  - src/components/features/documents/DocumentCenter.vue
  - src/components/features/recycleBin/RecycleBin.vue
autonomous: true
requirements:
  - DARK-01
  - DARK-02
  - DARK-03
  - DOC-03

must_haves:
  truths:
    - Icons use light colors in dark theme (#a1a1aa default, #f4f4f5 hover)
    - Text uses light colors in dark theme (#f4f4f5 primary, #a1a1aa secondary)
    - UI components standardize to CSS variables
    - Visual consistency across all components
  artifacts:
    - path: src/components/ui/Icon.vue
      provides: Icon component with default dark theme color
      contains: class="text-[var(--text-secondary)]"
    - path: src/components/ui/Button.vue
      provides: Button with CSS variable styling
      contains: var(--bg-
    - path: src/components/ui/Input.vue
      provides: Input with CSS variable styling
      contains: var(--text
    - path: src/components/ui/Select.vue
      provides: Select with CSS variable styling
      contains: var(--text
  key_links:
    - from: Icon.vue
      to: style.css
      via: CSS variable class default
      pattern: text-\\[var\\(--text-secondary\\)\\]
    - from: Button.vue, Input.vue, Select.vue
      to: style.css
      via: CSS variables
      pattern: var\\(--bg-\\|var\\(--text-
---

<objective>
Standardize UI components and icon colors to use CSS variables for dark theme consistency.

Purpose: Implement DARK-01 (icon colors), DARK-02 (text colors), DARK-03 (visual consistency), and DOC-03 (component standardization).
Output: UI components updated to use CSS variables, icons with proper default colors.
</objective>

<execution_context>
@C:/Users/10345/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/10345/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-dark-ui-optimization/01-UI-SPEC.md
@.planning/phases/01-dark-ui-optimization/01-RESEARCH.md
@src/style.css
@src/components/ui/Icon.vue
@src/components/ui/Button.vue
@src/components/ui/Input.vue
@src/components/ui/Select.vue

# Icon Color Spec (from UI-SPEC.md):
| Context | Color | Hex |
|---------|-------|-----|
| Default state | #a1a1aa | text-secondary |
| Hover state | #f4f4f5 | text-primary |
| Active/Selected | #6366f1 | accent-primary |
| Disabled | #52525b | text-disabled |

# Text Color Spec:
| Role | Color | Hex |
|------|-------|-----|
| Primary | #f4f4f5 | --text-primary |
| Secondary | #a1a1aa | --text-secondary |
| Tertiary | #71717a | --text-tertiary |
| Disabled | #52525b | --text-disabled |

# CSS Variables (from style.css):
--text-primary: #f4f4f5
--text-secondary: #a1a1aa
--text-tertiary: #71717a
--text-disabled: #52525b
--bg-primary: #08080c
--bg-secondary: #0f0f14
--bg-tertiary: #16161d
--border-default: rgba(255, 255, 255, 0.1)
--accent-primary: #6366f1
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add default CSS variable class to Icon component</name>
  <files>src/components/ui/Icon.vue</files>
  <read_first>
    - src/components/ui/Icon.vue (full file)
  </read_first>
  <action>
    Update Icon.vue to apply a default CSS variable class for dark theme consistency.

    Add a default class to the component props that uses the secondary text color:
    - Current: Icon accepts class prop but has no default
    - Target: Icon defaults to text-[var(--text-secondary)] when no class provided

    Modify the component definition to include a default class:
    ```vue
    <script setup lang="ts">
    // ... existing imports

    const props = withDefaults(defineProps<{
      name: string;
      size?: number;
      strokeWidth?: number;
      class?: string;
    }>(), {
      size: 20,
      strokeWidth: 2,
      class: "text-[var(--text-secondary)]"  // Default dark theme icon color
    });
    </script>
    ```

    This ensures all icons in the app will default to using the secondary text color (#a1a1aa), which provides good contrast on dark backgrounds.
  </action>
  <verify>
    <automated>grep -n "text-\\[var(--text-secondary)\\]" src/components/ui/Icon.vue</automated>
  </verify>
  <acceptance_criteria>
    - grep "text-\[var(--text-secondary)\]" returns match in Icon.vue
    - Icon.vue has withDefaults defining default class
    - Default class is "text-[var(--text-secondary)]"
  </acceptance_criteria>
  <done>Icon component now defaults to secondary text color for dark theme</done>
</task>

<task type="auto">
  <name>Task 2: Update Button component to use CSS variables</name>
  <files>src/components/ui/Button.vue</files>
  <read_first>
    - src/components/ui/Button.vue (full file)
    - src/style.css (CSS variables)
  </read_first>
  <action>
    Update Button.vue to replace hardcoded colors with CSS variables where applicable.

    Replace the variants object in Button.vue to use CSS variables:

    Current (uses hardcoded zinc/white):
    ```typescript
    const variants = {
      primary: "bg-[#1c1c26] text-zinc-200 border border-white/10 hover:bg-[#252532] hover:border-white/20",
      secondary: "bg-[#16161d] text-zinc-300 border border-white/8 hover:bg-[#1c1c26] hover:border-white/15",
      danger: "bg-red-500/15 text-red-400 border border-red-500/25 hover:bg-red-500/25",
      ghost: "bg-transparent text-zinc-400 hover:text-white hover:bg-white/5",
      gradient: "bg-gradient-to-r from-indigo-500 to-violet-600 text-white hover:from-indigo-600 hover:to-violet-700",
      outline: "bg-transparent text-zinc-300 border border-white/15 hover:bg-white/5 hover:border-white/25",
    };
    ```

    Target (use CSS variables where defined):
    ```typescript
    const variants = {
      primary: "bg-[var(--bg-elevated)] text-[var(--text-primary)] border border-[var(--border-default)] hover:bg-[var(--bg-tertiary)] hover:border-[var(--border-hover)]",
      secondary: "bg-[var(--bg-tertiary)] text-[var(--text-secondary)] border border-[var(--border-default)] hover:bg-[var(--bg-elevated)] hover:border-[var(--border-hover)]",
      danger: "bg-red-500/15 text-[var(--accent-red)] border border-red-500/25 hover:bg-red-500/25",
      ghost: "bg-transparent text-[var(--text-secondary)] hover:text-[var(--text-primary)] hover:bg-white/5",
      gradient: "bg-gradient-to-r from-[var(--accent-primary)] to-[var(--accent-secondary)] text-white hover:shadow-lg hover:shadow-[var(--accent-primary)]/20",
      outline: "bg-transparent text-[var(--text-primary)] border border-[var(--border-default)] hover:bg-white/5 hover:border-[var(--border-hover)]",
    };
    ```

    Keep gradient as is (Tailwind gradient syntax is cleaner), but add shadow using CSS variable.
  </action>
  <verify>
    <automated>grep -n "var(--bg-" src/components/ui/Button.vue && grep -n "var(--text-" src/components/ui/Button.vue</automated>
  </verify>
  <acceptance_criteria>
    - grep "var(--bg-" returns at least 3 matches in Button.vue
    - grep "var(--text-" returns at least 2 matches in Button.vue
    - Button variants use CSS variables for background, text, and border colors
  </acceptance_criteria>
  <done>Button component now uses CSS variables for styling</done>
</task>

<task type="auto">
  <name>Task 3: Update Input and Select components to use CSS variables</name>
  <files>src/components/ui/Input.vue, src/components/ui/Select.vue</files>
  <read_first>
    - src/components/ui/Input.vue (full file)
    - src/components/ui/Select.vue (full file)
    - src/style.css (CSS variables)
  </read_first>
  <action>
    Update Input.vue and Select.vue to use CSS variables.

    Input.vue changes:
    - Replace "bg-[#12121a]" with "bg-[var(--bg-tertiary)]"
    - Replace "text-zinc-100" with "text-[var(--text-primary)]"
    - Replace "placeholder-zinc-500" with "placeholder-[var(--text-tertiary)]"
    - Replace "border-white/10" with "border-[var(--border-default)]"
    - Replace "focus:border-indigo-500" with "focus:border-[var(--accent-primary)]"

    Select.vue changes:
    - Replace "bg-[#1a1a25]" with "bg-[var(--bg-tertiary)]"
    - Replace "text-white" with "text-[var(--text-primary)]"
    - Replace "border-white/10" with "border-[var(--border-default)]"
    - Replace "focus:border-indigo-500" with "focus:border-[var(--accent-primary)]"

    Example Input.vue update:
    ```vue
    const inputClasses = computed(() => {
      const base = "w-full bg-[var(--bg-tertiary)] border text-[var(--text-primary)] placeholder-[var(--text-tertiary)] transition-all duration-200 focus:outline-none disabled:cursor-not-allowed disabled:opacity-50";
      const errorState = "border-red-500/50 focus:border-red-500 focus:ring-2 focus:ring-red-500/20";
      const normalState = "border-[var(--border-default)] focus:border-[var(--accent-primary)] focus:ring-2 focus:ring-[var(--accent-primary)]/20";
      return `${base} ${errorState ? errorState : normalState}`;
    });
    ```
  </action>
  <verify>
    <automated>grep -n "var(--bg-" src/components/ui/Input.vue && grep -n "var(--text-" src/components/ui/Input.vue && grep -n "var(--bg-" src/components/ui/Select.vue</automated>
  </verify>
  <acceptance_criteria>
    - grep "var(--bg-" returns at least 1 match in Input.vue
    - grep "var(--text-" returns at least 2 matches in Input.vue
    - grep "var(--bg-" returns at least 1 match in Select.vue
    - grep "var(--text-" returns at least 1 match in Select.vue
  </acceptance_criteria>
  <done>Input and Select components now use CSS variables</done>
</task>

</tasks>

<verification>
- Icon component: Icons should appear in light gray (#a1a1aa) by default on dark backgrounds
- Button: Buttons should use consistent styling with other components via CSS variables
- Input/Select: Form inputs should use consistent colors via CSS variables
- Visual check: All components should have consistent dark theme colors
</verification>

<success_criteria>
1. Icon.vue has default class="text-[var(--text-secondary)]"
2. Button.vue uses CSS variables for bg, text, border colors
3. Input.vue uses CSS variables for bg, text, border colors
4. Select.vue uses CSS variables for bg, text, border colors
5. All components visually consistent in dark theme
</success_criteria>

<output>
After completion, create `.planning/phases/01-dark-ui-optimization/01-02-SUMMARY.md`
</output>
