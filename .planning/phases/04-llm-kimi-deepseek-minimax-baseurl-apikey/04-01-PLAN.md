---
phase: 04-llm-kimi-deepseek-minimax-baseurl-apikey
plan: '01'
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/src/llm/openai.rs
  - src-tauri/src/llm/storage.rs
  - src-tauri/src/commands/llm.rs
autonomous: true
requirements:
  - LLM-07
  - LLM-08

must_haves:
  truths:
    - "User can configure base_url for each model"
    - "User can configure model_name for each model"
    - "Chat uses the configured base_url for API calls"
  artifacts:
    - path: "src-tauri/src/llm/openai.rs"
      provides: "OpenAIClient with configurable base_url"
      contains: "base_url: String"
    - path: "src-tauri/src/llm/storage.rs"
      provides: "Model config storage functions"
      exports: ["store_model_config", "retrieve_model_config"]
    - path: "src-tauri/src/commands/llm.rs"
      provides: "Tauri commands for model config"
      exports: ["llm_save_model_config", "llm_get_model_config"]
  key_links:
    - from: "src-tauri/src/commands/llm.rs"
      to: "src-tauri/src/llm/openai.rs"
      via: "OpenAIClient::new() passes base_url"
    - from: "src-tauri/src/commands/llm.rs"
      to: "src-tauri/src/llm/storage.rs"
      via: "store_model_config() / retrieve_model_config()"
---

<objective>
Extend the Rust backend to support configurable base_url and model_name for each LLM provider (Kimi, DeepSeek, MiniMax). This enables the OpenAI-compatible client to call different API endpoints.

Purpose: Allow users to configure Chinese LLM providers with their own API endpoints.
Output: Modified OpenAIClient, storage functions, and Tauri commands.
</objective>

<execution_context>
@D:/Projects/Tauri/ProjectFlow/.planning/phases/04-llm-kimi-deepseek-minimax-baseurl-apikey/04-RESEARCH.md
</execution_context>

<context>
@D:/Projects/Tauri/ProjectFlow/src-tauri/src/llm/openai.rs
@D:/Projects/Tauri/ProjectFlow/src-tauri/src/llm/storage.rs
@D:/Projects/Tauri/ProjectFlow/src-tauri/src/commands/llm.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend OpenAIClient with base_url parameter</name>
  <files>src-tauri/src/llm/openai.rs</files>
  <read_first>src-tauri/src/llm/openai.rs</read_first>
  <action>
    Modify OpenAIClient::new() to accept an optional base_url parameter:
    1. Add `base_url: String` field to the struct
    2. Update new() signature: `pub fn new(api_key: String, model: Option<String>, base_url: Option<String>) -> Self`
    3. Default base_url: "https://api.openai.com" if not provided
    4. Update stream_chat() and chat() to use self.base_url instead of hardcoded URL
    5. Build URL as: format!("{}/v1/chat/completions", self.base_url)
  </action>
  <acceptance_criteria>
    - OpenAIClient struct has base_url field
    - new() accepts base_url parameter with default
    - stream_chat() uses configured base_url
  </acceptance_criteria>
  <verify>
    <automated>cargo check --package projectflow 2>&1 | head -20</automated>
  </verify>
  <done>OpenAIClient accepts base_url parameter and uses it for API calls</done>
</task>

<task type="auto">
  <name>Task 2: Add model config storage functions</name>
  <files>src-tauri/src/llm/storage.rs</files>
  <read_first>src-tauri/src/llm/storage.rs</read_first>
  <action>
    Extend storage.rs to store model configuration (base_url, model_name) alongside api_key:
    1. Add ModelConfig struct with base_url and model_name fields
    2. Add store_model_config(model_id: &str, config: &ModelConfig) -> Result<(), String>
    3. Add retrieve_model_config(model_id: &str) -> Result<Option<ModelConfig>, String>
    4. Use the same keyring service but with "_config" suffix for config entries
    5. Serialize ModelConfig as JSON before encrypting
  </action>
  <acceptance_criteria>
    - ModelConfig struct exists with base_url and model_name
    - store_model_config encrypts and stores config
    - retrieve_model_config retrieves and decrypts config
  </acceptance_criteria>
  <verify>
    <automated>cargo check --package projectflow 2>&1 | head -20</automated>
  </verify>
  <done>Model config can be stored and retrieved securely</done>
</task>

<task type="auto">
  <name>Task 3: Add Tauri commands for model config</name>
  <files>src-tauri/src/commands/llm.rs</files>
  <read_first>src-tauri/src/commands/llm.rs</read_first>
  <action>
    Add new Tauri commands for model configuration:
    1. Add ModelConfigRequest struct with model_id, base_url, model_name, api_key fields
    2. Add llm_save_model_config command: stores both api_key and model config
    3. Add llm_get_model_config command: retrieves model config (without api_key)
    4. Modify llm_chat to retrieve model config and pass base_url to OpenAIClient
    5. Add Chinese models to llm_get_models (Kimi, DeepSeek, MiniMax)
  </action>
  <acceptance_criteria>
    - llm_save_model_config command exists and works
    - llm_get_model_config command exists and works
    - llm_chat uses base_url from config
    - llm_get_models returns Chinese models
  </acceptance_criteria>
  <verify>
    <automated>cargo check --package projectflow 2>&1 | head -20</automated>
  </verify>
  <done>Backend supports saving/loading model config with base_url</done>
</task>

</tasks>

<verification>
Backend compiles without errors. All new functions are exported from llm module.
</verification>

<success_criteria>
1. OpenAIClient accepts base_url parameter
2. Model config can be stored/retrieved via keyring
3. Tauri commands for config CRUD work
4. llm_chat uses configured base_url
</success_criteria>

<output>
After completion, create `.planning/phases/04-llm-kimi-deepseek-minimax-baseurl-apikey/04-01-SUMMARY.md`
</output>
