---
phase: 04-llm-kimi-deepseek-minimax-baseurl-apikey
plan: '02'
type: execute
wave: 2
depends_on:
  - '04-01'
files_modified:
  - src/stores/llmStore.ts
  - src/lib/api.ts
  - src/components/features/llm/LlmSettings.vue
autonomous: true
requirements:
  - LLM-09

must_haves:
  truths:
    - "User can select from Chinese LLM providers (Kimi, DeepSeek, MiniMax)"
    - "User can configure base_url for each model"
    - "User can configure model_name for each model"
    - "Settings UI shows configured status for each model"
  artifacts:
    - path: "src/stores/llmStore.ts"
      provides: "Model config state management"
      contains: "modelConfigs"
    - path: "src/lib/api.ts"
      provides: "API methods for model config"
      exports: ["saveModelConfig", "getModelConfig"]
    - path: "src/components/features/llm/LlmSettings.vue"
      provides: "UI for model selection and config"
      contains: "base_url input, model_name input, model dropdown"
  key_links:
    - from: "src/components/features/llm/LlmSettings.vue"
      to: "src/stores/llmStore.ts"
      via: "saveModelConfig() action"
    - from: "src/stores/llmStore.ts"
      to: "src/lib/api.ts"
      via: "llmApi.saveModelConfig()"
---

<objective>
Extend the Vue frontend to support model selection and configuration for Chinese LLM providers. Users can select Kimi, DeepSeek, or MiniMax, and configure base_url and model_name for each.

Purpose: Provide UI for users to configure and switch between Chinese LLM providers.
Output: Updated llmStore, api.ts, and LlmSettings.vue.
</objective>

<execution_context>
@D:/Projects/Tauri/ProjectFlow/.planning/phases/04-llm-kimi-deepseek-minimax-baseurl-apikey/04-RESEARCH.md
</execution_context>

<context>
@D:/Projects/Tauri/ProjectFlow/src/stores/llmStore.ts
@D:/Projects/Tauri/ProjectFlow/src/lib/api.ts
@D:/Projects/Tauri/ProjectFlow/src/components/features/llm/LlmSettings.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend llmStore with model config state</name>
  <files>src/stores/llmStore.ts</files>
  <read_first>src/stores/llmStore.ts</read_first>
  <action>
    Extend llmStore to support model configuration:
    1. Add modelConfigs ref to store config for each model
    2. Add selectedModelId to track current selection
    3. Add saveModelConfig(modelId, config) action
    4. Add getModelConfig(modelId) action
    5. Update sendMessage to use selectedModelId for config lookup
    6. Export ModelConfig type: { base_url: string, model_name: string }
  </action>
  <acceptance_criteria>
    - llmStore has modelConfigs state
    - saveModelConfig action exists
    - sendMessage uses configured model
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck 2>&1 | head -20</automated>
  </verify>
  <done>llmStore manages model configuration state</done>
</task>

<task type="auto">
  <name>Task 2: Add model config API methods</name>
  <files>src/lib/api.ts</files>
  <read_first>src/lib/api.ts</read_first>
  <action>
    Add model configuration API methods to api.ts:
    1. Add ModelConfig type: { model_id: string, base_url: string, model_name: string, api_key: string }
    2. Add saveModelConfig(config: ModelConfig): Promise<void>
    3. Add getModelConfig(modelId: string): Promise<ModelConfig | null>
    4. Add getModels() returns all available models including Chinese
  </action>
  <acceptance_criteria>
    - ModelConfig type is exported
    - saveModelConfig calls llm_save_model_config
    - getModelConfig calls llm_get_model_config
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck 2>&1 | head -20</automated>
  </verify>
  <done>Frontend API has methods for model config CRUD</done>
</task>

<task type="auto">
  <name>Task 3: Update LlmSettings.vue with config UI</name>
  <files>src/components/features/llm/LlmSettings.vue</files>
  <read_first>src/components/features/llm/LlmSettings.vue</read_first>
  <action>
    Update LlmSettings.vue to support Chinese LLM configuration:
    1. Replace models array with DEFAULT_MODEL_CONFIGS:
       - kimi: { name: 'Kimi', base_url: 'https://api.moonshot.cn', default_model: 'moonshot-v1-8k' }
       - deepseek: { name: 'DeepSeek', base_url: 'https://api.deepseek.com', default_model: 'deepseek-chat' }
       - minimax: { name: 'MiniMax', base_url: 'https://api.minimax.chat', default_model: 'abab6.5s-chat' }
       - (Keep existing GPT models)
    2. Add base_url input field (text input, pre-filled with defaults)
    3. Add model_name input field (text input, pre-filled with defaults)
    4. Update saveKey to call saveModelConfig with all fields
    5. Update model selector to show provider name + model name
    6. Show configured status per model
  </action>
  <acceptance_criteria>
    - Model dropdown shows all providers (Kimi, DeepSeek, MiniMax, GPT)
    - base_url input is pre-filled with provider defaults
    - model_name input is pre-filled with default model
    - Save stores complete config (base_url + model_name + api_key)
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck 2>&1 | head -20</automated>
  </verify>
  <done>Settings UI allows configuration of Chinese LLM providers</done>
</task>

</tasks>

<verification>
Frontend compiles without TypeScript errors. Settings UI shows model dropdown with Chinese providers.
</verification>

<success_criteria>
1. Users can select Kimi, DeepSeek, MiniMax from dropdown
2. base_url is pre-filled with defaults, editable
3. model_name is pre-filled with defaults, editable
4. API key is saved with config
5. Chat uses selected model's configured endpoint
</success_criteria>

<output>
After completion, create `.planning/phases/04-llm-kimi-deepseek-minimax-baseurl-apikey/04-02-SUMMARY.md`
</output>
